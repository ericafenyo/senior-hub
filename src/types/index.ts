export type Team = {
  id: string;
  name: string;
  description: string;
  createdAt: string;
  updatedAt: string;
}

export type Role = {
  id: string;
  name: string;
  slug: string;
  description: string;
}
